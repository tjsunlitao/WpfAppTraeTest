<UserControl x:Class="WpfAppTraeTest.Views.CalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F7FA">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Text="系统标定" FontSize="20" FontWeight="Bold" Foreground="#3182CE" Margin="0,0,0,16"/>

        <!-- 标定内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 相机标定 -->
                <Border Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="相机标定" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,12"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F7FAFC" Padding="12" CornerRadius="6" Margin="0,0,6,0">
                                <StackPanel>
                                    <TextBlock Text="单相机标定" Foreground="#2D3748" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBlock Text="对每个相机进行内参标定" Foreground="#718096" FontSize="12" Margin="0,0,0,12"/>
                                    <Button Content="开始标定" Background="#3182CE" Foreground="White" BorderThickness="0" Height="32" Cursor="Hand"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Background="#F7FAFC" Padding="12" CornerRadius="6" Margin="3,0">
                                <StackPanel>
                                    <TextBlock Text="手眼标定" Foreground="#2D3748" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBlock Text="相机与机械手坐标系标定" Foreground="#718096" FontSize="12" Margin="0,0,0,12"/>
                                    <Button Content="开始标定" Background="#3182CE" Foreground="White" BorderThickness="0" Height="32" Cursor="Hand"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#F7FAFC" Padding="12" CornerRadius="6" Margin="6,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="多相机联合标定" Foreground="#2D3748" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBlock Text="多相机坐标系统一标定" Foreground="#718096" FontSize="12" Margin="0,0,0,12"/>
                                    <Button Content="开始标定" Background="#3182CE" Foreground="White" BorderThickness="0" Height="32" Cursor="Hand"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 标定参数 -->
                <Border Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="标定参数设置" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,12"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                                <TextBlock Text="标定板类型" Foreground="#718096" FontSize="12" Margin="0,0,0,4"/>
                                <ComboBox Background="#FFFFFF" Foreground="#2D3748" BorderBrush="#E2E8F0" Height="28">
                                    <ComboBoxItem Content="棋盘格" IsSelected="True"/>
                                    <ComboBoxItem Content="圆点阵"/>
                                    <ComboBoxItem Content="ArUco"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,12">
                                <TextBlock Text="标定板尺寸 (mm)" Foreground="#718096" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox Text="20.0" Background="#FFFFFF" Foreground="#2D3748" BorderBrush="#E2E8F0" Height="28" Padding="8,4"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Text="角点数量 (行x列)" Foreground="#718096" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox Text="9 x 6" Background="#FFFFFF" Foreground="#2D3748" BorderBrush="#E2E8F0" Height="28" Padding="8,4"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="采集图像数量" Foreground="#718096" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox Text="20" Background="#FFFFFF" Foreground="#2D3748" BorderBrush="#E2E8F0" Height="28" Padding="8,4"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 标定结果 -->
                <Border Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="标定结果" FontSize="16" FontWeight="SemiBold" Foreground="#2D3748" Margin="0,0,0,12"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#F7FAFC" Padding="12" CornerRadius="6" Margin="0,0,6,0">
                                <StackPanel>
                                    <TextBlock Text="重投影误差" Foreground="#718096" FontSize="12"/>
                                    <TextBlock Text="0.12 pixels" Foreground="#38A169" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                                    <TextBlock Text="标定精度: 优秀" Foreground="#38A169" FontSize="11" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Background="#F7FAFC" Padding="12" CornerRadius="6" Margin="6,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="标定时间" Foreground="#718096" FontSize="12"/>
                                    <TextBlock Text="2025-03-01" Foreground="#2D3748" FontSize="18" FontWeight="Bold" Margin="0,4,0,0"/>
                                    <TextBlock Text="14:32:18" Foreground="#718096" FontSize="11" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>