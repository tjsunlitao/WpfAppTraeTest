<Window x:Class="WpfAppTraeTest.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppTraeTest.Views"
        xmlns:vm="clr-namespace:WpfAppTraeTest.ViewModels"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="900"
        Width="1600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#F5F7FA"
        Foreground="#202124"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="HeaderPanel" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E3E7"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="24,12"/>
        </Style>

        <Style x:Key="CameraContainer" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E3E7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>

        <Style x:Key="SidePanel" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E3E7"/>
            <Setter Property="BorderThickness" Value="1,0,0,0"/>
            <Setter Property="Padding" Value="18"/>
        </Style>

        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#F2F4F7"/>
            <Setter Property="Foreground" Value="#202124"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D5D9DE"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F0FE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D2E3FC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StartBtn" TargetType="Button" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#E6F4EA"/>
            <Setter Property="BorderBrush" Value="#34A853"/>
            <Setter Property="Foreground" Value="#188038"/>
        </Style>

        <Style x:Key="StopBtn" TargetType="Button" BasedOn="{StaticResource ActionBtn}">
            <Setter Property="Background" Value="#FCE8E6"/>
            <Setter Property="BorderBrush" Value="#EA4335"/>
            <Setter Property="Foreground" Value="#C5221F"/>
        </Style>

        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#5F6368"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8EAED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeaderPanel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="M12,18.17L8.83,15L7.42,16.41L12,21L16.59,16.41L15.17,15L12,18.17M12,5.83L15.17,9L16.58,7.59L12,3L7.41,7.59L8.83,9L12,5.83Z"
                          Fill="#1A73E8" Width="24" Height="24" Stretch="Uniform" Margin="0,0,10,0"/>
                    <TextBlock Text="视觉对位主界面" FontSize="20" FontWeight="Bold" Foreground="#1A73E8"/>
                    <Border Background="#EEF3FD" CornerRadius="12" Padding="10,2" Margin="20,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{Binding StatusBrush}" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding SystemStatus}" VerticalAlignment="Center" FontSize="12" Foreground="#1A73E8"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="0" FontFamily="Marlett" Style="{StaticResource WindowButton}" Click="MinimizeButton_Click"/>
                    <Button Content="1" FontFamily="Marlett" Style="{StaticResource WindowButton}" Click="MaximizeButton_Click"/>
                    <Button Content="r" FontFamily="Marlett" Style="{StaticResource WindowButton}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <UniformGrid Columns="3" Rows="2" Margin="12">
                <Border Style="{StaticResource CameraContainer}">
                    <Grid>
                        <TextBlock Text="相机1 - 左前" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#5F6368" FontSize="12"/>
                        <Grid Background="#F2F4F7" Margin="0,28,0,0">
                            <TextBlock Text="无图像" Foreground="#9AA0A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
                <Border Style="{StaticResource CameraContainer}">
                    <Grid>
                        <TextBlock Text="相机2 - 右前" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#5F6368" FontSize="12"/>
                        <Grid Background="#F2F4F7" Margin="0,28,0,0">
                            <TextBlock Text="无图像" Foreground="#9AA0A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
                <Border Style="{StaticResource CameraContainer}">
                    <Grid>
                        <TextBlock Text="相机3 - 左后" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#5F6368" FontSize="12"/>
                        <Grid Background="#F2F4F7" Margin="0,28,0,0">
                            <TextBlock Text="无图像" Foreground="#9AA0A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
                <Border Style="{StaticResource CameraContainer}">
                    <Grid>
                        <TextBlock Text="相机4 - 右后" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#5F6368" FontSize="12"/>
                        <Grid Background="#F2F4F7" Margin="0,28,0,0">
                            <TextBlock Text="无图像" Foreground="#9AA0A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
                <Border Style="{StaticResource CameraContainer}">
                    <Grid>
                        <TextBlock Text="相机5 - 俯视" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#5F6368" FontSize="12"/>
                        <Grid Background="#F2F4F7" Margin="0,28,0,0">
                            <TextBlock Text="无图像" Foreground="#9AA0A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>
                <Border Style="{StaticResource CameraContainer}" BorderBrush="#1A73E8" BorderThickness="2">
                    <Grid Background="#F2F4F7">
                        <TextBlock Text="综合判定" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Foreground="#1A73E8" FontSize="12" FontWeight="Bold"/>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="合格" Foreground="#188038" FontSize="42" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="节拍：2.45s" Foreground="#5F6368" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </UniformGrid>

            <Border Grid.Column="1" Style="{StaticResource SidePanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="系统控制" Foreground="#1A73E8" Margin="0,0,0,10" FontWeight="Bold"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="启动" Style="{StaticResource StartBtn}" Margin="0,0,5,0"/>
                            <Button Grid.Column="1" Content="停止" Style="{StaticResource StopBtn}" Margin="5,0,0,0"/>
                        </Grid>
                        <Button Content="自动标定" Style="{StaticResource ActionBtn}"/>
                        <Button Content="手动触发" Style="{StaticResource ActionBtn}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,0,0,20">
                        <TextBlock Text="对位结果" Foreground="#1A73E8" Margin="0,0,0,10" FontWeight="Bold"/>
                        <Border Background="#F2F4F7" CornerRadius="8" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="ΔX" Foreground="#5F6368" HorizontalAlignment="Center"/>
                                    <TextBlock Text="0.012" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="ΔY" Foreground="#5F6368" HorizontalAlignment="Center"/>
                                    <TextBlock Text="-0.005" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="Δθ" Foreground="#5F6368" HorizontalAlignment="Center"/>
                                    <TextBlock Text="0.002°" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <ListView Background="Transparent" BorderThickness="0" ItemsSource="{Binding Cameras}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E3E7" BorderThickness="0,0,0,1" Padding="6,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Margin="0,0,10,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="#5F6368" FontSize="11"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>

                    <Grid Grid.Row="3" Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="过程日志" Foreground="#1A73E8" Margin="0,0,0,5" FontWeight="Bold"/>
                        <Border Grid.Row="1" Background="#F2F4F7" CornerRadius="8">
                            <ListBox ItemsSource="{Binding Logs}" Background="Transparent" Foreground="#1F4E79" FontFamily="Consolas" FontSize="11" BorderThickness="0"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E0E3E7" BorderThickness="0,1,0,0" Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="VisionPro 引擎：9.8 SR1" Foreground="#5F6368" FontSize="11"/>
                <TextBlock Grid.Column="2" Text="用户：管理员 | 2026-02-28 12:00:00" Foreground="#5F6368" FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</Window>
